[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Configure reporting options
addopts = 
    --verbose
    --cov=similarity
    --cov-report=term

# Customize output
console_output_style = progress
